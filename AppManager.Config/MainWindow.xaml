<Window x:Class="AppManager.Config.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppManager.Config"
        mc:Ignorable="d"
        Title="App Manager" Height="600" Width="600">
	<Window.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="ListTemplates/Nav1ListItemTemplate.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</Window.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="auto"/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		
		<!-- Top Row with Title and Browser Button -->
		<Grid Grid.Row="0" Grid.Column="0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>
			
			<!-- App Manager Title and Profile Selector -->
			<StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0">
				<Label Content="App Manager" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
				<Label Content="Profile:" VerticalAlignment="Center" Margin="0,0,5,0"/>
				<ComboBox x:Name="ProfileSelector" 
				          Width="150" 
				          Height="24" 
				          VerticalAlignment="Center"
				          SelectionChanged="ProfileSelector_SelectionChanged"
				          ToolTip="Select active profile"/>
			</StackPanel>
			
			<!-- Browser and Data Folder Buttons in Center -->
			<StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
				
				<Button x:Name="DataFolderButton" 
				        Content="📁 Data" 
				        Width="100" 
				        Height="28" 
				        Margin="5" 
				        Click="DataFolderButton_Click"
				        Background="#FFC107"
				        Foreground="White"
				        FontWeight="Bold"
				        BorderThickness="0"
				        ToolTip="Open data folder with profiles and settings"/>
			</StackPanel>
			
			<!-- Console Panel on Right -->
			<StackPanel x:Name="ConsolePanel" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" />
		</Grid>
		

		<ScrollViewer Grid.Row="2" Grid.Column="0" Margin="10" VerticalScrollBarVisibility="Auto">
			<Frame x:Name="MainFrame"/>
		</ScrollViewer>


		<DockPanel Grid.Row="3" Grid.Column="0" Background="#F0F0F0" Height="32">
			<Label Content="Status: Ready" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0"/>
			<StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0">
				<Label Content="Version: " Margin="0" Padding="0" />
				<Label Content="{Binding Version, RelativeSource={RelativeSource AncestorType=Window}}" Margin="0" Padding="0" />
			</StackPanel>
			
			<Label Content="© 2024 App Manager" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,10,0"/>
		</DockPanel>
	</Grid>
</Window>
