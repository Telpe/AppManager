<parametercontrols:BaseParameterControl 
				x:Class="AppManager.Config.ParameterControls.TagsParameter"
				xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
				xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
				xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
				xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
				xmlns:parametercontrols="clr-namespace:AppManager.Config.ParameterControls" 
				d:DataContext="{d:DesignInstance Type=parametercontrols:TagsParameter}"
				mc:Ignorable="d" >
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0" Text="{Binding HeaderText}" FontWeight="Bold" Margin="0,0,0,5"/>

		<TextBlock Grid.Row="1" Text="{Binding LabelText}" VerticalAlignment="Center" Margin="0,0,0,5"/>

		<ItemsControl Grid.Row="2" x:Name="TagsItemsControl" ItemsSource="{Binding DisplayItems}">
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<Grid Margin="0,2">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="10"/>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="Auto"/>
						</Grid.ColumnDefinitions>

						<TextBox Grid.Column="0" 
                                 Text="{Binding TagKey, UpdateSourceTrigger=LostFocus}" 
                                 LostFocus="CategoryTextBox_LostFocus"
                                 KeyDown="TextBox_KeyDown"
                                 Tag="{Binding}"/>

						<TextBlock Grid.Column="1" Text=":" VerticalAlignment="Center" HorizontalAlignment="Center"/>

						<TextBox Grid.Column="2" 
                                 Text="{Binding TagValue, UpdateSourceTrigger=LostFocus}" 
                                 LostFocus="ValueTextBox_LostFocus"
                                 KeyDown="TextBox_KeyDown"
                                 Tag="{Binding}"/>

						<Button Grid.Column="3" 
                                Content="&#215;" 
                                Width="20" 
                                Height="20" 
                                Margin="5,0,0,0"
                                Click="DeleteButton_Click"
                                Tag="{Binding}"
                                Visibility="{Binding CanDelete, Converter={StaticResource BoolToVisibilityConverter}}"
                                ToolTip="Delete this tag"/>
					</Grid>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
	</Grid>
</parametercontrols:BaseParameterControl>