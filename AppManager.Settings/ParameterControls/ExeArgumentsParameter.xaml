<parametercontrols:BaseParameterControl 
                x:Class="AppManager.Settings.ParameterControls.ExeArgumentsParameter"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                xmlns:parametercontrols="clr-namespace:AppManager.Settings.ParameterControls" 
                mc:Ignorable="d" 
                d:DesignHeight="120" d:DesignWidth="400">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0" 
                   Text="{Binding HeaderText}" 
                   FontWeight="Bold" 
                   Margin="0,0,0,5"/>

		<TextBlock Grid.Row="1" 
                   Text="{Binding LabelText}" 
                   Margin="0,0,0,3"/>

		<TextBox Grid.Row="2" 
                 x:Name="ArgumentsTextBox"
                 Text="{Binding Value}"
                 TextChanged="ArgumentsTextBox_TextChanged"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 MaxLength="32698"
                 MinHeight="60"/>
	</Grid>
</parametercontrols:BaseParameterControl>