<parametercontrols:BaseParameterControl 
	x:Class="AppManager.Settings.ParameterControls.FilePathParameter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:parametercontrols="clr-namespace:AppManager.Settings.ParameterControls" 
			 d:DataContext="{d:DesignInstance Type=parametercontrols:FilePathParameter}"
			 mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="400">
	
	<Grid Margin="5">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<Grid Grid.Row="0" Margin="0,0,0,5">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<TextBox x:Name="FilePathTextBox" Grid.Column="0" Height="25" 
                        Text="{Binding Value, Mode=TwoWay}"
                        />
			<Button Grid.Column="1" Content="Browse..." Width="70" Height="25" Margin="5,0,0,0"
                    Click="BrowseButton_Click"/>
			<Button Grid.Column="2" Content="Clear" Width="60" Height="25" Margin="5,0,0,0"
                    Click="ClearButton_Click"/>
		</Grid>

		<TextBlock x:Name="FileStatusTextBlock" Grid.Row="1" Margin="0,5,0,0" 
                    FontSize="10" Text="No file selected" 
                    Foreground="Gray"/>
	</Grid>
</parametercontrols:BaseParameterControl>