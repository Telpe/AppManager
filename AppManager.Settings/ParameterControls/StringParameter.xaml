<parametercontrols:BaseParameterControl 
				x:Class="AppManager.Settings.ParameterControls.StringParameter"
				xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
				xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
				xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
				xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
				xmlns:parametercontrols="clr-namespace:AppManager.Settings.ParameterControls" 
				d:DataContext="{d:DesignInstance Type=parametercontrols:StringParameter}"
				mc:Ignorable="d" 
				d:DesignHeight="120" d:DesignWidth="400">

	<Grid Margin="5">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0" 
                   Text="{Binding HeaderText}" 
                   FontWeight="Bold" 
                   Margin="0,0,0,5"/>

		<TextBlock Grid.Row="1" 
                   Text="{Binding LabelText}" 
                   Margin="0,0,0,3"/>

		<TextBox    x:Name="ValueTextBox"
                    Grid.Row="2"
                    Text="{Binding Value, Mode=TwoWay}"
                    TextChanged="ValueTextBox_TextChanged"
                    Height="25"
                    VerticalAlignment="Top"/>
	</Grid>
</parametercontrols:BaseParameterControl>