<parametercontrols:BaseParameterControl 
	x:Class="AppManager.Config.ParameterControls.FilePathParameter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:parametercontrols="clr-namespace:AppManager.Config.ParameterControls" 
			 d:DataContext="{d:DesignInstance Type=parametercontrols:FilePathParameter}"
			 mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="400">
	
	<Grid Margin="5">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        
        <TextBox    Grid.Row="0"
			Grid.Column="0"
			Style="{StaticResource ReadOnlyLabelTextBoxStyle}"
			Text="{Binding LabelText, Mode=OneTime}" 
			Margin="0,0,3,0"/>

        <TextBox x:Name="FilePathTextBox" Grid.Row="0" Grid.Column="1" Height="25" 
                    Text="{Binding Value, Mode=TwoWay}" />

        <Button Grid.Column="2" Grid.Row="0" Content="Browse..." Width="70" Height="25" Margin="5,0,0,0"
                Click="BrowseButton_Click"/>

        <Button Grid.Column="3" Grid.Row="0" Content="Clear" Width="60" Height="25" Margin="5,0,0,0"
                Click="ClearButton_Click"/>

        <TextBox    Grid.Row="0"
		    Grid.Column="4"
		    Grid.RowSpan="3"
			Style="{StaticResource ReadOnlyDescriptionTextBoxStyle}"
		    Text="{Binding Description, Mode=OneTime}"
		    Margin="0,0,0,0"/>

        <TextBlock x:Name="FileStatusTextBlock" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4" Margin="0,5,0,0" 
                    FontSize="10" Text="No file selected" 
                    Foreground="Gray"/>
	</Grid>
</parametercontrols:BaseParameterControl>