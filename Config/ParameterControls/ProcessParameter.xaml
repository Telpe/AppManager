<parametercontrols:BaseParameterControl
				x:Class="AppManager.Config.ParameterControls.ProcessParameter"
				xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                xmlns:parametercontrols="clr-namespace:AppManager.Config.ParameterControls" 
                d:DataContext="{d:DesignInstance Type=parametercontrols:ProcessParameter}"
                mc:Ignorable="d" 
                d:DesignHeight="120" d:DesignWidth="400">
  
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <TextBox    Grid.Row="0"
			Grid.Column="0"
			Style="{StaticResource ReadOnlyLabelTextBoxStyle}"
			Text="{Binding LabelText, Mode=OneTime}" 
			Margin="0,0,3,0"/>
        
        <!-- Process Input Row -->
		<Grid Grid.Row="0" Grid.Column="1" Margin="0,0,0,5">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<TextBox x:Name="ProcessNameTextBox" 
                     Grid.Column="0"
                     Text="{Binding Value}"
                     Margin="0,0,5,0"/>

			<Button Grid.Column="1" 
                    Content="Browse..." 
                    Width="80" 
                    Height="23"
                    Margin="0,0,5,0"
                    Click="BrowseButton_Click"/>

			<Button Grid.Column="2" 
                    Content="Refresh" 
                    Width="60" 
                    Height="23"
                    Margin="0,0,5,0"
                    Click="RefreshButton_Click"/>

			<Button Grid.Column="3" 
                    Content="Clear" 
                    Width="50" 
                    Height="23"
                    Click="ClearButton_Click"/>
		</Grid>

        <TextBox    Grid.Row="0"
                    Grid.Column="2"
                    Grid.RowSpan="3"
	                Style="{StaticResource ReadOnlyDescriptionTextBoxStyle}"
                    Text="{Binding Description, Mode=OneTime}"
                    Margin="0,0,0,0"/>
        
		<!-- Status Display -->
		<TextBlock Grid.Row="1" 
                   Grid.Column="0"
                   Grid.ColumnSpan="2"
                   x:Name="ProcessStatusTextBlock"
                   Text="No process specified" 
                   FontSize="12" 
                   Foreground="Gray"
                   Margin="0,0,0,5"/>

        
    </Grid>
</parametercontrols:BaseParameterControl>